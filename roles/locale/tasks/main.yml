---
- name: enable locale in locale.gen
  lineinfile:
    dest: /etc/locale.gen
    regexp: '^(#|){{ locale }}'
    line: '{{ locale }}'

- name: generate locale
  command: locale-gen

- name: set locale
  copy:
    dest: '/etc/locale.conf'
    content: 'LANG={{ locale }}'
